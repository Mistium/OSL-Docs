# s3

> Amazon S3 client

```javascript
import "osl/s3"
```

## Methods

- `s3.new(cfg)` → `s3Client`

## Returned object: `s3Client`

Returned by `s3` methods; call these on the value you get back.

- `s3Client.sign(method, key, payload, query)` → `string`
- `s3Client.put(input)` → `object`
- `s3Client.get(input)` → `object`
- `s3Client.remove(input)` → `object`
- `s3Client.presign(input)` → `object`
