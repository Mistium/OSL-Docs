# colors

> Color utilities

```javascript
import "osl/colors"
```

## Methods

- `colors.rgb(r, g, b)` → `color`
- `colors.gray(v)` → `color`
- `colors.nrgba(r, g, b, a)` → `color`
- `colors.hex(hex)` → `color`
