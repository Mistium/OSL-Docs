# template

> Lightweight {{ }} string templating with interpolation, loops and conditionals

```javascript
import "osl/template"
```

## Methods

- `template.render(tmpl, data)` → `string`
- `template.renderHTML(tmpl, data)` → `string`
