# json

> JSON utilities

```javascript
import "osl/json"
```

## Methods

- `json.parse(data, options)` → `Result`
- `json.stringify(data)` → `string`
- `json.format(data)` → `string`
- `json.isValid(data)` → `boolean`
- `json.isObject(data)` → `boolean`
- `json.isArray(data)` → `boolean`
