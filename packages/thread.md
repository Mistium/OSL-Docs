# thread

```javascript
import "osl/thread"
```

## Methods

- `thread.new(fn, ...args)` → `Thread`
- `thread.wait()` → `any`
- `thread.isDone()` → `boolean`
- `thread.age()` → `number`
- `thread.waitAll(threads)` → `array`
