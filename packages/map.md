# map

```javascript
import "osl/map"
```

## Methods

- `map.set(k, v)` → `Map`
- `map.get(k)` → `any`
- `map.delete(k)`
- `map.size()` → `number`
- `map.clear()`
- `map.getKeys()` → `array`
- `map.getValues()` → `array`
