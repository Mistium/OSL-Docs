# win-buttons

```osl
import "std:win-buttons"
```

Importing this package installs close, minimise, and maximise controls into an
OSL window. It exposes no callable package methods and requires a graphical
display. Drawing and typed hit-test tables use the same fixed control order.
