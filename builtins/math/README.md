# Math

Standalone math helpers that are built into the compiler. They work on plain
numbers without importing the [`math`](../../packages/math.md) package.

For per-number operations such as `.round()`, `.sqrt()` or `.clamp()`, see the
[number methods](../../methods/numbers/README.md). The pages below cover the
global functions.
