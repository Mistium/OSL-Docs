# Image Editing

OSL can edit a data URI or a blank image through Canvas or URI commands. Use Canvas for individual pixels or performance-sensitive work. Use URI commands for whole-image filters; they are slower.
